<%render partial: "partials/crumb", locals: {active: @box.name, passive: [link_to("Home", customer_boxes_path)]}%>
<div class="row">
  <%=render partial: "partials/horizontal_box_tabs", locals: {box: @box, active: "settings"}%>
  <%=render partial: "partials/box_tabs", locals: {box: @box, active: "settings"}%>

  <div class="col-12 d-md-none p-3">
    <%=render partial: "edit_form", locals: {box:@box}%>
  </div>

  <div class="col-10 d-none d-md-block p-4">
    <%=render partial: "edit_form", locals: {box:@box}%>
  </div>

</div>
