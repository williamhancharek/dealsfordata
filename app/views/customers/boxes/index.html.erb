<%# <%render partial: "partials/crumb", locals: {active: "Home"}%>

<%=createWelcomeMessage(box_count: @box_count, foreign_box: @foreign_box)%>
<%=render partial: "partials/title", locals: {title:"Check your request boxes for recent recommendations!"}%>
<p class="mb-3">Check out your request boxes for new recommendations!</p>
<div class="row" style="padding-top: 15px;"></div>

<div class="card-columns">
  <%=render partial: 'partials/box', collection: @boxes%>
</div>

<!-- Button trigger modal -->

<button type="button" class="btn btn-dark font-weight-bold btn-sm mb-3" data-toggle="modal" data-target="#newBoxModal">
  Create new box
</button>

<!-- Modal -->
<%=render partial: "partials/create_new_box", locals: {box: @box}%>
