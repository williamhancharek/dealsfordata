New offer for Box <%=@box.name%>
===============================================
Offer title: <%=@offer.title if @offer.title.present?%>
Description: <%=@offer.description if @offer.description.present?%>
Commentary:<%=@offer.received_commentary if @offer.received_commentary.present?%>
<%="currently #{@offer.current_price}" if @offer.current_price.present?%> <%="discounted from #{@offer.retail_price}" if @offer.retail_price.present?%>

To login to the site, just follow this link: <%= @url %>.

Have a happy day!
